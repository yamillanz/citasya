# Initialize OpenSpec for all supported tools

```bash
cd your-project
openspec init --tools all
```

This configures integrations for all 20+ supported AI coding tools at once.

Use **openspec-explore** or **openspec-new** to start working.
