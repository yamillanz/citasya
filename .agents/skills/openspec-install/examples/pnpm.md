# Install OpenSpec via pnpm

```bash
# Install globally
pnpm add -g @fission-ai/openspec@latest

# Verify
openspec --version
```

Then use **openspec-initial** to run `openspec init` in your project.
