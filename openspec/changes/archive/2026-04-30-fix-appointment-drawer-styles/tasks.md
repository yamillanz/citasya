## 1. Fix Drawer Styles

- [x] 1.1 Add proper background, border-radius and shadow to `.status-drawer` using design tokens
- [x] 1.2 Style `.p-drawer-header` with linen background, bottom border, and proper padding
- [x] 1.3 Style `.p-drawer-content` and `.p-drawer-footer` with consistent padding
- [x] 1.4 Fix `.amount-input` price-input layout and input styling to use brand focus states
- [x] 1.5 Ensure `.drawer-footer` buttons use existing `.btn-primary`, `.btn-secondary` overrides
- [ ] 1.6 Verify responsive behavior on mobile viewports
