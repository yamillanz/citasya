## 1. Hide internal scrollbar in mobile calendar

- [x] 1.1 Add CSS rules to hide `.fc-scroller` scrollbar in mobile view (`max-width: 768px`) using `scrollbar-width: none` and `::-webkit-scrollbar { display: none }`.
- [x] 1.2 Verify that touch scrolling still works on the calendar in mobile view.
