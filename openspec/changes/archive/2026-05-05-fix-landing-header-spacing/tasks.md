# Tasks: Fix Landing Header Spacing

## Tasks

- [x] Wrap `desktop-nav` and `auth-buttons` in `.header-right` container (HTML)
- [x] Add `.header-right` styles with flex, gap `--space-2xl`, hidden below 1024px
- [x] Reduce `.desktop-nav` gap from `--space-xl` to `--space-lg`
- [x] Remove media query wrapper from `.auth-buttons` (visibility handled by parent)
- [x] Build and verify no compilation errors
