## 1. Fix mobile header button widths

- [x] 1.1 Add `flex-wrap: wrap` and `justify-content: flex-start` to `.header-actions` in mobile media query.
- [x] 1.2 Force `width: auto` on `.p-button` elements inside `.header-actions` for mobile viewports.
